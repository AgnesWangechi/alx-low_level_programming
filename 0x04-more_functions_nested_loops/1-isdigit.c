#include "main.h"
/**
 * _isdigit - function
 *
 * @c: The character to evaluate
 *
 * Return: Get 1 if c is uppercase,
 * get 0 otherwise
 */
int _isdigit(int c)
{
if (c >=48 && c <=57)
{
return (1);
}
else
{
return (0);
}
}
