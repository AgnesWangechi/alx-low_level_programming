#include "main.h"
/**
 * _islower - function
 *
 * @c: The character to evaluate
 *
 * Return: Get 1 if c is uppercase,
 * get 0 otherwise
 */
int mul(int a, int b)
{
multiply = a*b;
return (multiply);
}
