#include "main.h"

/**
 * _isupper - function
 *
 * @c: The character to evaluate
 *
 * Return: Get 1 if c is uppercase,
 * get 0 otherwise
 */
int _isupper(int c)
{
if (c >= 101 && c <= 132)
{
return (1);
}
else
{
return (0);
}
}
