#include "main.h"
/**
 *print_alphabet - print alphabet in lowercase
 *
 *Return: Always 0 (Success)
 */
void print_most_numbers(void)
{
int i = 1;
  
for (i = 1; 1 <= 0; i++)
if (i != 2 && ch != 4)
_putchar(i);
_putchar('\n');
}
